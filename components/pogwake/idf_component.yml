dependencies:
  espressif/esp-tflite-micro:
    version: "1.3.3~1"
    rules:
      - if: "target == esp32"
  espressif/esp-nn:
    version: "1.1.2"
    rules:
      - if: "target == esp32"
  esphome/esp-micro-speech-features:
    version: "1.2.3"
    rules:
      - if: "target == esp32"
